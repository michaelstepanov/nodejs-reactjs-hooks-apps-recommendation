const Collection = require('../../classes/collection/Collection');

/**
 * Even though the class is empty it's used for filtering - filters may be defined for a specific Collection child class
 */
class App extends Collection {}

module.exports = App;
